apiVersion: v1
kind: Service
metadata:
  name: transactions-service
  namespace: transactions
  labels:
    app: transactions-service
spec:
  selector:
    app: transactions-service
  ports:
    - port: 8003
      targetPort: 8003
  type: ClusterIP
