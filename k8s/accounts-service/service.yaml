apiVersion: v1
kind: Service
metadata:
  name: accounts-service
  namespace: accounts
  labels:
    app: accounts-service
spec:
  selector:
    app: accounts-service
  ports:
    - port: 8002
      targetPort: 8002
  type: ClusterIP
