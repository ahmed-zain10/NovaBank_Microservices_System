apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts-service
  namespace: accounts
  labels:
    app: accounts-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: accounts-service
  template:
    metadata:
      labels:
        app: accounts-service
    spec:
      serviceAccountName: accounts-service-sa
      containers:
        - name: accounts-service
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/novabank/dev/accounts-service:${IMAGE_TAG}
          ports:
            - containerPort: 8002
          env:
            - name: SECRET_NAME
              value: "novabank/dev/rds/accounts"
            - name: AWS_REGION
              value: "${AWS_REGION}"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 20
            periodSeconds: 20
