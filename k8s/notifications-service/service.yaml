apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: notifications
  labels:
    app: notifications-service
spec:
  selector:
    app: notifications-service
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
